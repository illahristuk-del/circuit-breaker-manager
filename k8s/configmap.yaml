apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-config 
  namespace: resilience-platform
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "fastapi_db"
  DB_USER: "postgres"
  PING_INTERVAL_DEFAULT: "5"
